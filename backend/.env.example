NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USER=cogep
DB_PASSWORD=cogep
DB_NAME=cogep

JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=1d

# Notificações WhatsApp via Twilio
# - NOTIFICATIONS_ENABLED=false: usa MockProvider (console.log), seguro para demo
# - NOTIFICATIONS_ENABLED=true + credenciais Twilio: envia mensagens reais
# Sandbox Twilio: https://console.twilio.com/ (Messaging → Try it out → WhatsApp)
NOTIFICATIONS_ENABLED=false
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
PUBLIC_APP_URL=http://localhost:4200
